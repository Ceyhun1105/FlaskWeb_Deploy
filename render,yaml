services:
  - type: web
    name: main
    env: python
    plan: free  # veya başka bir plan
    buildCommand: "pip install -r requirements.txt"
    startCommand: "waitress-serve --host 0.0.0.0 --port $PORT wsgi:app"
    routes:
      - src: /api/*
        dest: /api
